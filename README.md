# X Files Reference

A separate editable React/Vite recreation based on the supplied X-Files reference screen recording. It is intentionally independent from the existing X-Files project.

## Run

```bat
npm install
npm run dev
```

## Main editable files
- `src/App.jsx` — page content and structure
- `src/App.css` — full visual system and responsive layout
